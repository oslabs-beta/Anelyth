version: '3.8'

services:
  web:
    build: anelyth/anelyth:v1.0.0
    ports:
      - "3000:3000"
    environment:
      - DB_ENDPOINT=${DB_ENDPOINT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
